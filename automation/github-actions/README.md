# github-actions

Source-of-truth copies of CI/CD workflow definitions. GitHub only executes workflows placed under `.github/workflows/` at the repo root — treat this folder as the authored/reviewed version and keep it in sync with what's actually deployed there.
