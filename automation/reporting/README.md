# reporting

Scripts that generate recurring reports (weekly summaries, program metrics) for distribution via `email/`. Interactive, always-current views belong in `/dashboards` instead — this folder is for point-in-time generated reports.
