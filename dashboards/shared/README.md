# shared

Shared UI components, chart primitives, and data-fetching utilities used by `coach/`, `athlete/`, and `admin/`. Keep dashboard-specific logic out of here — only put something in `shared/` once at least two dashboards need it.
