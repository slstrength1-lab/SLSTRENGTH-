# agents

Operating notes for the Claude agent roster: how each agent defined in `/claude-agents` is actually used in daily workflow, who owns it, and what triggers it. The agent *definitions* (system prompts, responsibilities) live in `/claude-agents`; this folder is the human-facing usage guide.
