# docs

Standing reference documentation: org structure, role definitions, glossary of program-specific terms, and any policy that doesn't belong to a single system. If a document needs to be updated every time something changes, it probably belongs in `roadmap/` or `meeting-notes/` instead.
