# handoffs

Structured handoff notes — between shifts, between staff, or between a human and an agent — so context isn't lost when work changes hands. One file per handoff, named `YYYY-MM-DD-<topic>.md`.
