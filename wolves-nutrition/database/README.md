# database

Schema definitions, migrations, and seed data for the Wolves Nutrition data model. Treat this as the human-readable source of truth for the schema; `supabase/` holds the Supabase-specific project config and generated artifacts that implement it.
