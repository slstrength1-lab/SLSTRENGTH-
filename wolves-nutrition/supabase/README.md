# supabase

Supabase project configuration: edge functions, generated TypeScript types, and local dev config (`supabase/config.toml` once initialized). Migrations authored here should stay in sync with `database/`.
